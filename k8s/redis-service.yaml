apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: kubequeue
spec:
  type: ClusterIP  # Internal service, accessible only within cluster
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379  # Redis default port
      targetPort: 6379
